[package]
name = "lib-macros"
version = "0.1.0"
publish = false
authors.workspace = true
edition.workspace = true

[features]
proc = ["dep:proc"]

[dependencies]
proc = { path = "./proc", package = "lib-macros-proc", optional = true }

[lints]
workspace = true
