[package]
name = "ad_platform-infrastructure-persistence-postgres"
version = "0.1.0"
authors.workspace = true
edition.workspace = true

[dependencies]
lib = { workspace = true, features = [
    "infrastructure_persistence_postgres"
] }
kernel = { path = "../../../kernel", package = "ad_platform-kernel" }

async-trait.workspace = true
thiserror.workspace = true
tracing.workspace = true
uuid.workspace = true

sqlx = { workspace = true, features = [
    "runtime-tokio",
    "postgres",
    "uuid"
] }

[lints]
workspace = true
